import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    label: {
        marginBottom: 10,
        fontSize: 15,
        fontWeight: "700"
    },

    input: {
        borderWidth: 1,
        padding: 10,
        borderRadius: 10
    },  

});

export default styles;