import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    button: {
        flexGrow: 1,
        marginHorizontal: 10,
        padding: 10,
        borderRadius: 5
    },

    text: {
        textAlign: "center",
        fontWeight: "700"
    }

});

export default styles;