import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    book: {
        padding: 10,
        borderWidth: 1,
        borderRadius: 10,
    },

    title: {
        fontWeight: "700"
    },

    id: {
        fontSize: 12
    } 

});

export default styles;